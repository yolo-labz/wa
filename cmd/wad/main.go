package main

import (
	"fmt"
	"os"
)

func main() {
	// Placeholder until Phase 3 (T016) adds the full composition root.
	fmt.Fprintln(os.Stderr, "wad: not yet implemented — see feature 006 Phase 3")
	os.Exit(64)
}
