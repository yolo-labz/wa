coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        target: 80%
        threshold: 5%

ignore:
  - "cmd/**"
  - "**/*_test.go"
  - "internal/adapters/secondary/memory/**"
  - "internal/porttest/**"

comment:
  layout: "reach, diff, flags, files"
  behavior: default
  require_changes: true
